import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class CustomTowButtonAlert extends StatelessWidget {

  Widget build(BuildContext context) {
    return Scaffold(
        body: Column(
          children: [
            AlertDialog(
            title: Text('عربي كم '),
                actions: [
                ]),
          ],
        ));
  }
}